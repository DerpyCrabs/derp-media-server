# E2E coverage gap matrix (React ↔ Solid)

Use this checklist to track **user-visible** behavior that should eventually have **both** a React e2e (`tests/e2e/`) and a Solid e2e (`tests/e2e-solid/`). Add rows as you discover gaps; point each row at the spec file that should own the test.

**Automation:** `bun run e2e:diff-specs` lists React-only and Solid-only spec basenames (exit code 1 if any React spec has no Solid twin).

## File browser (admin home)

| Affordance                                     | React (`components/file-list.tsx`) | Solid (`solid/src/FileBrowser.tsx`)    | Covered in e2e                                        |
| ---------------------------------------------- | ---------------------------------- | -------------------------------------- | ----------------------------------------------------- |
| List/grid toggle                               | Yes                                | Yes                                    | `navigation.spec`                                     |
| Breadcrumbs, `..`, virtual folders             | Yes                                | Yes                                    | `navigation.spec`                                     |
| Favorites / Most Played                        | Yes                                | Yes                                    | `navigation.spec`                                     |
| Create folder/file, rename, delete, move, copy | Yes                                | Yes                                    | `editable-folders.spec`                               |
| Context share / KB / icon                      | Yes                                | Yes                                    | `shares-manage`, `knowledge-base`                     |
| Upload menu (files)                            | Yes                                | Yes                                    | `upload.spec`                                         |
| Upload menu (folder)                           | Yes                                | Yes                                    | `file-browser-misc.spec`                              |
| Upload drop zone on listing                    | Yes                                | Yes                                    | `file-browser-misc.spec`                              |
| Clipboard paste                                | Yes                                | Yes (`FileBrowser` + `PasteDialog`)    | `file-browser-misc.spec` (`clipboard paste` describe) |
| Theme switcher (light/dark/system)             | Yes                                | Yes (`ThemeSwitcher` in `FileBrowser`) | `file-browser-misc.spec`                              |

## Workspace / share

**Spec parity:** Every React `workspace-*.spec.ts` has a Solid twin under `tests/e2e-solid/` (same describe/test names; Solid imports shared helpers from `tests/e2e/workspace-layout-helpers.ts` where needed). `bun run e2e:diff-specs` reports no React-only workspace basenames.

**Share dialog:** Escape dismisses dialog — `file-browser-misc.spec` (admin file list). **SSE:** React and Solid `sse-live-updates.spec` use the same pattern (unique `SharedContent` filenames per run, `gotoWithSSE` stream/console race, share create-file via placeholder locators, delete via `alertdialog`).

### Workspace feature audit (React vs Solid vs e2e)

| Affordance                                                                                             | React                                                     | Solid                                                                          | React e2e                                                                       | Solid e2e                    | Notes                                                                                                                      |
| ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Window drag / resize, z-order, focus                                                                   | `components/workspace/window.tsx`, `WorkspacePage`        | `WorkspaceWindowChrome`, `workspace-snap-resize-handles`                       | `workspace-layout-chrome`, `workspace-layout-snap-resize`, `workspace-controls` | same basenames               | Core parity; Solid uses pointer-driven chrome, not `react-rnd`.                                                            |
| Edge snap + snap preview                                                                               | `useSnapZones`, `SnapPreview`                             | `detectSnapZone`, `data-snap-preview`                                          | `workspace-layout-snap-resize`, `workspace-layout-chrome`                       | same                         |                                                                                                                            |
| Tiling layout picker (right-click maximize)                                                            | `TilingLayoutPicker` + `visibleSnapLayoutIds`             | `WorkspaceTilingPicker` (always `ALL_SNAP_LAYOUT_IDS`)                         | `workspace-layout-chrome`, `workspace-controls`                                 | same                         | React hides templates per `useWorkspaceSnapLayoutVisibility` (from theme settings). Solid always shows full template set.  |
| Tab merge / split / drop slots / detach                                                                | `WindowGroup`, `useWorkspace`                             | `tab-group-ops`, `merge-target`, chrome                                        | `workspace-controls`                                                            | same                         |                                                                                                                            |
| Taskbar: open browser, per-window row, focus / minimize / close                                        | `Layout`, `WorkspaceWindowTaskbarRows`                    | bottom taskbar in `WorkspacePage`                                              | `workspace-controls`                                                            | same                         |                                                                                                                            |
| Pinned taskbar: add, open, unpin (context menu); persist admin + share                                 | `layout.tsx` (pins draggable for file DnD)                | pins + drag (`setFileDragData`), `data-taskbar-pin`                            | `workspace-taskbar-pins` (+ pin → folder cross-window move)                     | same                         | Pin drag-as-file parity; e2e: `dragging pinned file to folder in another browser moves the file`.                          |
| Cross-window file move (editable roots)                                                                | `share-file-browser` / workspace browsers                 | `WorkspaceBrowserPane`                                                         | `workspace-cross-dnd`                                                           | same                         |                                                                                                                            |
| Drop file/folder onto tab bar                                                                          | same                                                      | `dropFileToTabBar`                                                             | `workspace-cross-dnd`                                                           | same                         |                                                                                                                            |
| Browser + viewers (image/PDF/text/markdown/edit)                                                       | workspace file/viewer components                          | `WorkspaceBrowserPane`, `WorkspaceViewerPane`, `WorkspacePlayerPane`           | `workspace-viewers`, `workspace-controls`                                       | same                         |                                                                                                                            |
| Video player window reuse                                                                              | `useWorkspace` + playback store                           | same store / `WorkspacePlayerPane`                                             | `workspace-controls`                                                            | same                         |                                                                                                                            |
| Session isolation (`?ws=` UUID)                                                                        | `useWorkspaceSessionUrl`                                  | `navigateSearchParams` in `WorkspacePage`                                      | `workspace-layout-sessions`                                                     | same                         | Both inject `?ws=` when missing (`replace`); tests assert per-param isolation.                                             |
| **Named layout presets** (save/apply/delete, `?preset=`, dirty “Modified” badge, revert/sync baseline) | `WorkspaceNamedLayoutMenu`, `use-workspace` baseline APIs | `WorkspaceNamedLayoutMenu`, baseline signals in `WorkspacePage`                | `workspace-named-layouts`                                                       | `workspace-named-layouts`    | Solid share workspace persists layout immediately; admin uses debounced persist to avoid `?preset=` vs localStorage races. |
| **Taskbar `AudioPlayer`** (audio playback chrome, show video)                                          | `WorkspacePage` taskbar slot                              | `WorkspaceTaskbarAudio`                                                        | `workspace-taskbar-chrome` (mp3 track in taskbar)                               | same                         | Optional: extend specs for “Show video” / audio-only from workspace.                                                       |
| **Theme switcher** on workspace taskbar                                                                | `ThemeSwitcher` `variant='taskbar'` (Settings dialog)     | Mode + palette in `WorkspaceTaskbarSettings` (gear dialog)                     | `workspace-taskbar-chrome` (dark persists after reload)                         | same                         | React dialog also includes snap-template visibility; Solid tiling picker still shows full template set.                    |
| **Open file target** (new tab vs new window from settings)                                             | `getWorkspaceFileOpenTarget()` in `WorkspacePage`         | `WorkspaceTaskbarSettings` + `getWorkspaceFileOpenTarget()` in `WorkspacePage` | `workspace-file-open-target`                                                    | `workspace-file-open-target` | Taskbar **Settings** dialog (Solid) mirrors React’s workspace theme switcher entry point for this control only.            |

### Suggested priority (next PR-sized chunks)

1. **Taskbar audio + theme** — Baseline: `workspace-taskbar-chrome` (track label + settings dark mode). Optional: more taskbar audio / “Show video” workspace flows.
2. **Pin drag-as-file** — Done: Solid pins use `setFileDragData`; `workspace-taskbar-pins` covers cross-window move onto a folder row.
3. **`getWorkspaceFileOpenTarget`** — Done: `workspace-file-open-target` (React + Solid).

Re-scan after UI changes; add rows when a new control ships without a test.

## Maintenance

- After adding a React spec, copy or adapt to `tests/e2e-solid/` and add the basename to `tests/run-batches-solid.ts` (≤6 batches).
- Update this table when a row becomes fully covered or when new affordances appear.
- **Playwright `reuseExistingServer: true`:** If something already responds on the test `baseURL` port, Playwright **reuses** that process and **does not** apply `webServer.env` (`UI_FRAMEWORK`, `CONFIG_PATH`, `BATCH_ID` media dir, etc.). A long-running dev server on **5973/5974** vs a batch port **9201–9206** explains “green locally, red in CI” or vice versa. Stop the stray process or use a free port so Playwright starts the configured server.
